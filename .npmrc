# 프라이빗 npm 레지스트리 설정
# GitHub Packages를 사용하는 경우
# @thinksys:registry=https://npm.pkg.github.com
# //npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

# 또는 자체 프라이빗 레지스트리를 사용하는 경우
# registry=https://your-private-registry.com
# //your-private-registry.com/:_authToken=${NPM_TOKEN}

# npmjs.org에 프라이빗 패키지로 배포하는 경우 (유료)
# 이 경우 별도 설정이 필요하지 않음
